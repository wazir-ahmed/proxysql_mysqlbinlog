name: Release
run-name: Release ${{ github.event.release.tag_name }}

on:
  release:
    types:
      - published

permissions:
  contents: write

jobs:
  package:
    name: package (${{ matrix.distro }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        distro:
          - centos9
          - centos10
          - debian12
          - debian13
          - ubuntu22
          - ubuntu24
    steps:
      - name: Check out source at release tag
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}
          fetch-depth: 0

      - name: Build ${{ matrix.distro }} package
        run: make ${{ matrix.distro }}

      - name: List built artifacts
        run: ls -l binaries/

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: package-${{ matrix.distro }}
          path: |
            binaries/*.deb
            binaries/*.rpm
          if-no-files-found: error

      - name: Upload package to GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          shopt -s nullglob
          assets=( binaries/*.deb binaries/*.rpm )
          if [ ${#assets[@]} -eq 0 ]; then
            echo "no package assets to upload"
            exit 1
          fi
          gh release upload "${{ github.event.release.tag_name }}" "${assets[@]}" --clobber

  container:
    name: container (${{ matrix.distro }})
    runs-on: ubuntu-latest
    needs: package
    strategy:
      fail-fast: false
      matrix:
        distro:
          - centos9
          - centos10
          - debian12
          - debian13
          - ubuntu22
          - ubuntu24
    permissions:
      packages: write
    steps:
      - name: Check out source at release tag
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name }}
          fetch-depth: 0

      - name: Download package
        uses: actions/download-artifact@v4
        with:
          name: package-${{ matrix.distro }}
          path: binaries/

      - name: Log into GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        run: make -C docker/images mysqlbinlog-${{ matrix.distro }} PUSH=1 IMAGE_PREFIX=ghcr.io/${{ github.repository_owner }}/proxysql-mysqlbinlog
